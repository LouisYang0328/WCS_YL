<Window x:Name="点位历史表更记录Window" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" d:DesignHeight="750" d:DesignWidth="1750" mc:Ignorable="d" x:Class="GZ.Projects.WCS_Template.TracedBackQR" xmlns:local="clr-namespace:GZ.Projects.WCS_Template">
  <Window.Resources>
    <!--文本标签Label-->
    <Style x:Key="CurrentLabel" TargetType="{x:Type Label}">
      <Setter Property="HorizontalAlignment" Value="Right" />
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="FontSize" Value="15" />
    </Style>
    <!--按钮-->
    <Style x:Key="CurrentButton" TargetType="{x:Type Button}">
      <Setter Property="Background" Value="#FF00796B" />
      <Setter Property="BorderBrush" Value="#FF00796B" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="FontSize" Value="20" />
    </Style>
    <!--文本框-->
    <Style x:Key="CurrentTextBox" TargetType="{x:Type TextBox}">
      <Setter Property="MinWidth" Value="100" />
      <Setter Property="Margin" Value="3" />
      <Setter Property="FontSize" Value="20" />
      <Setter Property="HorizontalAlignment" Value="Left" />
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="HorizontalContentAlignment" Value="Center" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
    <!--文本标签Label-->
    <Style x:Key="CurrentDatePicker" TargetType="{x:Type DatePicker}">
      <Setter Property="MinWidth" Value="150" />
      <Setter Property="HorizontalAlignment" Value="Left" />
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="FontSize" Value="18" />
    </Style>
  </Window.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="0.7*" />
      <RowDefinition Height="0.6*" />
      <RowDefinition Height="7.3*" />
    </Grid.RowDefinitions>
    <Border BorderBrush="Gray" BorderThickness="1" Grid.Row="0">
      <Grid Background="#FFD0CCCC" Grid.ColumnSpan="2" Grid.Row="0">
        <TextBlock Margin="10" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="30" FontWeight="Bold">
          日志管理/条码追溯
        </TextBlock>
      </Grid>
    </Border>
    <!--异常处理按钮-->
    <!--搜索条件-->
    <Grid Background="LightGray" Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="0.5*" />
        <ColumnDefinition Width="5.4*" />
        <ColumnDefinition Width="0.8*" />
        <ColumnDefinition Width="0.8*" />
        <ColumnDefinition Width="2*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition />
      </Grid.RowDefinitions>
      <TextBlock Margin="5" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="20" Text="搜索" Grid.Column="0" Grid.Row="0" />
      <TextBox Margin="5" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" BorderBrush="LightGreen" BorderThickness="1" FontSize="20" Text="{Binding Tag.UI.条码追溯.Text_QueryConditions}" Grid.Column="1" Grid.Row="0" />
      <Button Content="查询" Command="{Binding btn_TracedBackQR_Select}" Margin="10,3" FontSize="20" Style="{StaticResource CurrentButton}" Grid.Column="2" Grid.Row="0" />
      <Button Content="重置" Command="{Binding btn_TracedBackQR_Clear}" Margin="10,3" FontSize="20" Style="{StaticResource CurrentButton}" Grid.Column="3" Grid.Row="0" />
    </Grid>
    <!--数据展示列表-->
    <Grid Margin="3" Grid.Row="2">
      <RichTextBox Margin="5" AcceptsReturn="True" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" IsReadOnly="True" IsDocumentEnabled="True">
        <FlowDocument>
          <Paragraph>
            <Run Text="{Binding Tag.UI.条码追溯.Text_Massage}" />
          </Paragraph>
        </FlowDocument>
      </RichTextBox>
    </Grid>
  </Grid>
</Window>