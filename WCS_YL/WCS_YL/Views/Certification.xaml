<Window x:Class="GZ.Projects.WCS_Template.Certification" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:GZ.Projects.WCS_Template" Name="Certification_View" Width="350" Height="150" ResizeMode="NoResize" Title="软件退出确认" WindowStartupLocation="CenterScreen" mc:Ignorable="d">
  <Window.InputBindings>
    <KeyBinding Command="{Binding ConfirmCmd}" CommandParameter="{Binding ElementName=psw}" Gesture="Enter" Key="Enter" />
  </Window.InputBindings>
  <Window.Resources>
    <local:ObjectConvert x:Key="ObjectConvert" xmlns:local="clr-namespace:GZ.Projects.WCS_Template" />
    <Style x:Key="AddPageLabel" TargetType="Label">
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Margin" Value="10" />
    </Style>
    <!--文本框-->
    <Style x:Key="AddPagePassword" TargetType="PasswordBox">
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="HorizontalContentAlignment" Value="Left" />
      <Setter Property="VerticalAlignment" Value="Stretch" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Margin" Value="10" />
    </Style>
    <!--保存按钮-->
    <Style x:Key="AddPageCommitButton" TargetType="Button">
      <Setter Property="Width" Value="100" />
      <Setter Property="Height" Value="30" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Background" Value="#FF00796B" />
      <Setter Property="BorderBrush" Value="#FF00796B" />
      <Setter Property="Foreground" Value="White" />
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Cursor" Value="Hand" />
          <Setter Property="Background" Value="#007ACC" />
        </Trigger>
      </Style.Triggers>
    </Style>
    <!--关闭按钮-->
    <Style x:Key="AddPageCloseButton" TargetType="Button">
      <Setter Property="Width" Value="100" />
      <Setter Property="Height" Value="30" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Background" Value="#FF00796B" />
      <Setter Property="BorderBrush" Value="#FF00796B" />
      <Setter Property="Foreground" Value="White" />
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Cursor" Value="Hand" />
          <Setter Property="Background" Value="#007ACC" />
        </Trigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>
  <Grid>
    <!--页面内容：布局-->
    <Grid.RowDefinitions>
      <RowDefinition Height="60" />
      <RowDefinition Height="50" />
    </Grid.RowDefinitions>
    <!--页面内容：录入-->
    <Grid Grid.Row="0">
      <!--布局-->
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="100" />
        <ColumnDefinition Width="215" />
      </Grid.ColumnDefinitions>
      <!--轮胎条码-->
      <Label Style="{DynamicResource AddPageLabel}" Grid.Column="0" Grid.Row="0">
        认证码：
      </Label>
      <PasswordBox x:Name="psw" Height="35" Style="{DynamicResource AddPagePassword}" Grid.Column="1" Grid.Row="0" />
    </Grid>
    <!--操作按钮-->
    <Grid Grid.Row="1">
      <!--布局-->
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="0.25*" />
        <ColumnDefinition />
        <ColumnDefinition />
        <ColumnDefinition Width="0.25*" />
      </Grid.ColumnDefinitions>
      <!--确定按钮-->
      <Button Command="{Binding ConfirmCmd}" Style="{DynamicResource AddPageCommitButton}" Grid.Column="1">
        确定
        <Button.CommandParameter><MultiBinding Converter="{ StaticResource ResourceKey=ObjectConvert}"><Binding ElementName="psw" /><Binding ElementName="Certification_View" /></MultiBinding></Button.CommandParameter></Button>
      <!--重置按钮-->
      <Button Command="{Binding ResetCmd}" CommandParameter="{Binding ElementName=psw}" Style="{DynamicResource AddPageCloseButton}" Grid.Column="2">
        重置
      </Button>
    </Grid>
  </Grid>
</Window>